<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Item</title>
    <link rel="stylesheet" href="/styles/admin.css">
</head>
<body>
    <div class="container">
        <h1>Add New Fruit in Quiz</h1>
        <form action="/admin/add" method="POST">
            <div class="form-group">
                <label for="name">Fruit name:</label>
                <input type="text" id="fruit_name" name="fruit_name" required>
            </div>
            <div class="form-group">
                <label for="Color">Color:</label>
                <input type="text" id="fruit_color" name="fruit_color" required>
            </div>
            <% if (locals.message && locals.pass) { %>
                <div class="error-message" style="color: green;"> <%= locals.message %></div>
            <% } %>
            <% if (locals.message && !locals.pass) { %>
                <div class="success-message" style="color: red;"> <%= locals.message %></div>
            <% } %>
            <div class="button-container">
                <a href="/admin" class="back-button">Back to Admin Dashboard</a>
                <button type="submit" class="submit-button">Add Fruit</button>
            </div>
        </form>
    </div>
</body>
</html>